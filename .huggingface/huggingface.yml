sdk: gradio
python_version: 3.10

env:
  - OPENAI_API_KEY: ${secrets.OPENAI_API_KEY}

run:
  - pip install -r requirements.txt
  - pip install pytest pytest-cov
  - pytest --cov=tina_app test_tina.py
